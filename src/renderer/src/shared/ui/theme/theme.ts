import { UIStyles } from "../kit.ts";

export const theme = UIStyles.createTheme({
    palette: {
        primary: {
            main: '#16a085',
        },
        secondary: {
            main: '#e2e8f0',
        },
    },
});
