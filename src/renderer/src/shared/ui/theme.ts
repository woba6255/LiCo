import { UIStyles } from "./kit";

export const theme = UIStyles.createTheme({
    palette: {
        primary: {
            main: '#16a085',
        },
        secondary: {
            main: '#708090',
        },
    },
});
