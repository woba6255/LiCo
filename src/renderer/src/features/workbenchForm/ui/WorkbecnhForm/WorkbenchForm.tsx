import { UI } from "shared/ui";
import { WorkbenchNameField } from "./WorkbenchNameField.tsx";

export function WorkbenchForm() {
    return (
        <UI.Box>
            <WorkbenchNameField />
        </UI.Box>
    );
}
